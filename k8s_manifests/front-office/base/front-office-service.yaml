apiVersion: v1
kind: Service
metadata:
  name: front-office-app-service
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: "/api/all"
spec:
  type: NodePort
  selector:
    app: front-office-app
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
