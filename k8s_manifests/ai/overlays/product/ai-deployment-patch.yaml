apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-app
  namespace: default
  labels:
    env: product
spec:
  selector:
    matchLabels:
      app: ai-app
  template:
    metadata:
      labels:
        app: ai-app
