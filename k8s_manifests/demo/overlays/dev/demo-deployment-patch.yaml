apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app
  namespace: default
  labels:
    env: dev
spec:
  selector:
    matchLabels:
      app: demo-app
  template:
    spec:
      containers:
      - name: demo-app
        resources:
          requests:
            memory: 128Mi
          limits:
            memory: 256Mi