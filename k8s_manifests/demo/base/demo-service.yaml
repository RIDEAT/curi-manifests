apiVersion: v1
kind: Service
metadata:
  name: demo-app-service
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: "/api/all"
spec:
  type: ClusterIP
  selector:
    app: demo-app
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
