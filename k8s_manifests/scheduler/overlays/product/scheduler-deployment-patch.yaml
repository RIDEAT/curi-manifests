apiVersion: apps/v1
kind: Deployment
metadata:
  name: scheduler-app
  namespace: default
  labels:
    env: product
spec:
  selector:
    matchLabels:
      app: scheduler-app
  template:
    metadata:
      labels:
        app: scheduler-app
