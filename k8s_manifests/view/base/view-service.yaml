apiVersion: v1
kind: Service
metadata:
  name: view-app-service
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: "/api/all"
spec:
  type: NodePort
  selector:
    app: view-app
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
